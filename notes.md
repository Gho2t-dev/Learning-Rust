Notes

use `rustup doc` for local documentation that is available offline

Rusts variables are immutable (unchangeable) by default, but can be made mutable with "mut":

use cargo doc --open to open the documentation created by cargo for my specific project.

I DONT NEED TO KNOW EACH CRATE AND ITS PROPERTIES 

so looking it up either on google or cargo docs is the best way to use crates.